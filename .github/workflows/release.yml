name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  platform:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            platform: darwin-x64
          - os: macos-15-arm64
            platform: darwin-arm64
          - os: windows-2025
            platform: win32-x64
          - os: ubuntu-24.04
            platform: linux-x64
          - os: ubuntu-24.04-arm
            platform: linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: lukka/get-cmake@latest
        with:
          cmakeVersion: "~4.0"
          ninjaVersion: latest
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: extensions/vscode/package-lock.json
      - name: Record release toolchain
        shell: bash
        run: |
          cmake --version
          ninja --version
          c++ --version || true
          node --version
          git -C external/vcpkg rev-parse HEAD
      - name: Bootstrap vcpkg
        shell: bash
        run: |
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            external/vcpkg/bootstrap-vcpkg.bat -disableMetrics
          else
            external/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          fi
      - run: cmake --preset release
      - run: cmake --build --preset release
      - run: ctest --preset release --output-on-failure
      - name: Install release tree
        run: cmake --install build/release --prefix stage/email-markup
      - name: Build platform VSIX
        working-directory: extensions/vscode
        run: |
          npm ci
          npm test
          npm run package
      - name: Test VS Code extension host
        if: runner.os != 'Linux'
        working-directory: extensions/vscode
        run: npm run test:host
      - name: Test VS Code extension host on Linux
        if: runner.os == 'Linux'
        working-directory: extensions/vscode
        run: xvfb-run -a npm run test:host
      - name: Archive and checksum
        shell: bash
        run: |
          cd stage
          cmake -E tar cf "../email-markup-${{ matrix.platform }}.zip" --format=zip email-markup
          cd ..
          cmake -E sha256sum "email-markup-${{ matrix.platform }}.zip" > "email-markup-${{ matrix.platform }}.zip.sha256"
          cp extensions/vscode/email-markup-language-*.vsix "email-markup-language-${{ matrix.platform }}.vsix"
          cmake -E sha256sum "email-markup-language-${{ matrix.platform }}.vsix" > "email-markup-language-${{ matrix.platform }}.vsix.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: email-markup-${{ matrix.platform }}
          path: |
            email-markup-${{ matrix.platform }}.zip
            email-markup-${{ matrix.platform }}.zip.sha256
            email-markup-language-${{ matrix.platform }}.vsix
            email-markup-language-${{ matrix.platform }}.vsix.sha256

  publish:
    if: github.event_name == 'push'
    needs: platform
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: email-markup-*
          merge-multiple: true
          path: release
      - name: Verify checksums
        working-directory: release
        run: sha256sum --check *.sha256
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ github.ref_name }}" release/* --verify-tag --generate-notes
